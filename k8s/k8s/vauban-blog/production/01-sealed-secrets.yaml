# Production Sealed Secrets
# Generate with:
#   kubectl create secret generic vauban-blog-secrets \
#     --namespace=vauban-blog \
#     --from-literal=M2M_API_KEY=vb_prod_xxx \
#     --from-literal=CRON_SECRET=xxx \
#     --from-literal=ARWEAVE_WALLET_KEY=xxx \
#     --dry-run=client -o yaml | \
#     kubeseal --controller-name=sealed-secrets --controller-namespace=sealed-secrets -o yaml \
#     > 01-sealed-secrets.yaml
#
# IMPORTANT: Replace this placeholder with actual sealed secret!
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: vauban-blog-secrets
  namespace: vauban-blog
spec:
  encryptedData:
    M2M_API_KEY: AgBxxxxxxxxxx  # Replace with actual sealed value
    CRON_SECRET: AgBxxxxxxxxxx  # Replace with actual sealed value
    ARWEAVE_WALLET_KEY: AgBxxxxxxxxxx  # Replace with actual sealed value
  template:
    metadata:
      name: vauban-blog-secrets
      namespace: vauban-blog
    type: Opaque
