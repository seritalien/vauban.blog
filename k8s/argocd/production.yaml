# ArgoCD Application for production
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vauban-blog-production
  namespace: argocd
  labels:
    app: vauban-blog
    environment: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/seritalien/vauban.blog.git
    targetRevision: main
    path: k8s/k8s/vauban-blog/production

  destination:
    server: https://kubernetes.default.svc
    namespace: vauban-blog

  syncPolicy:
    # Manual sync for production - require explicit approval
    automated: null
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 2m

  # Production info
  info:
    - name: url
      value: https://bastion.vauban.tech
    - name: preview-url
      value: https://preview.bastion.vauban.tech
